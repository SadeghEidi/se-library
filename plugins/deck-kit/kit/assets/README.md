Put your own logo here and point `data-deck-logo` at it in the deck's
`index.html`. The runtime stamps it into the footer of every slide.

Nothing ships in this directory on purpose: a deck kit should not carry
someone else's marks.
